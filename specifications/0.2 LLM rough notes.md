In the previous example, we introduced the challenge of mathematical modeling, noting that it doesn't really represent wealth management reality. We'll use large language models to translate from the human narrative into mathematical modeling, making it clear to students that LLMs can already do this.

Start with prompt preparation, turning the investor's narrative from the previous lecture into JSON that can be used for Python portfolio optimization. Reuse all the code from the previous lecture related to mathematics, but build a bridge using an LLM that creates JSON, and then parse that JSON into the inputs needed for the Python code. This will demonstrate the reusability of the system.

The question is, how do we know that the large language model did it correctly? At this stage, I want you to generate a synthetic dataset of maybe 15-20 examples of how investor narratives are translated into these JSONs for mathematical optimization.

Then, use the Langfuse framework to run the evaluations. Find the most recent version of Langfuse and show how to use datasets in Langfuse, and also how to build the evaluators. I want to show students that there are some built-in evaluators available in Langfuse that we can use, and also how to build a custom one—an LLM as a judge—specifically targeting our challenge.

For LLM choice, allow for a choice of a large language model provider between OpenAI, Anthropic, and Gemini. Trace everything we do to Langfuse, so we can introduce the elements of LLMOps, but the main focus should be on evaluations and the quality of evaluations. I'll provide all the needed API keys and everything in the `.env` file.

As a result of this section, students should understand how to create datasets and use an LLM as a judge to evaluate LLM performance.