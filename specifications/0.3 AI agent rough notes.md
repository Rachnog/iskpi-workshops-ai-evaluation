In this last section, I want to emphasize the challenges that large language models have: they aren't trained with up-to-date data, and they're typically not really good at calculations or mathematics in general. In the real world, wealth managers need on-demand access to different data about the client and the market, and a deep understanding of specific instruments. You can't just compress everything into a large language model prompt. The question is, what should you even add there, and what shouldn't you?

With this in mind, I want to introduce the concept of AI agents and agentic AI. We'll keep the previous LLM as a decision-maker. Add to it portfolio optimization as a tool to be used by the agent. First, prompt,logic to transalate the user request into a portfolio optimization problem, then use the portfolio optimization tool to solve the problem. You should wrap it as a Langchain agent, trace everything to Langfuse.

Then, we'll build a second agent with more tools. It will have RAG as a retrieval tool and also a web search tool using DuckDuckGo because it's free. You should wrap it as a Langchain agent and use the most recent library version for it, to enable usage of the second agent as an evaluator for the previous agent. Use the dataset from the previous example in the RAG as a set of references for the evaluator agent.

For evaluation, design new scenarios from the investors that are more complex and require more tools to be used by the agent, potentially multi-hop reasoning.

I'm adding below examples of an agent-to-agent protocol and the AgentBeats framework. You should build a very minimalistic example of how these two agents—one that is an evaluator, which has tools, and one that is just a raw LLM call, as we did in the previous lecture—interact.

In the trace, I should clearly see how the evaluator agent is talking to the first agent, querying it, and judging it. Here, I want you to reuse the previous LLM as a judge prompt, but already in an agentic manner, where an agent is judging it. Adapt it as you see fit.

References for the A2A approach:

1. https://rdi.berkeley.edu/agentx-agentbeats - is the official website of the AgentBeats competition, high-level information
2. https://docs.agentbeats.dev/ - newer agentbeats official documentation main page, in the end might be more correct than the old one
3. https://docs.agentbeats.dev/tutorial/ - newer agentbeats official documentation tutorial page, in the end might be more correct than the old one
4. https://github.com/RDI-Foundation/agentbeats-tutorial - newer AgentBeats concepts, assessment design principles, and working examples, might be more correct than the old one
5. https://github.com/RDI-Foundation/agent-template - newer general A2A agent template, useful for building purple agents, might be more correct than the old one
6. https://github.com/RDI-Foundation/green-agent-template - newer template for building green agents compatible with the AgentBeats platform, might be more correct than the old one
7. https://github.com/a2aproject/A2A - A2A protocol GitHub repository
8. https://github.com/google/adk-python - Google Agent Development Kit Python SDK

In the end, also, provide an additional experimental implementaiton of the portfolio optimization agent using Claude and Claude SDK with the most recent version that uses skills. Decompose the prompt into skills of universe selection, portfolio optimization task translation via prompt and the portfolio optimization using Python code as well. 

Wrap it to A2A and evaluate it with the evaluator agent as well. Ideally we need to see that skills architecture is more robust than the previous implementation.